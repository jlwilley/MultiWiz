namespace MultiWiz.App.Overlays;

/// <summary>A small click-through badge (slot number and account name) at the top-left of a game window.</summary>
public partial class NameBadgeOverlay : OverlayWindowBase
{
    public NameBadgeOverlay()
    {
        InitializeComponent();
    }
}
